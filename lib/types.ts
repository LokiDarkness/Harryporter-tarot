export type Profile = { name:string; birthDate:string; gender?:string; birthTime?:string; birthplace?:string };
export type TarotCard = { id:string; name:string; symbol:string; keywords:string[]; upright:string; reversed:string };
